#      .
#     / V\
#   / `  /
#  <<   |    External Secret example for mautrix-googlechat.
#  /    |    Provide runtime secrets via a pre-created Secret.
#  /    |    Note: template/lint output may show missing-secret info until that Secret exists.
#

homeserver:
  domain: matrix.example.com

# Create runtime secret:
# kubectl -n <namespace> create secret generic mautrix-googlechat-runtime-secrets \
#   --from-literal=asToken="$(openssl rand -hex 32)" \
#   --from-literal=hsToken="$(openssl rand -hex 32)" \
#   --from-literal=provisioningSharedSecret="$(openssl rand -hex 32)"
registration:
  existingSecret: mautrix-googlechat-runtime-secrets
  managedSecret:
    enabled: false
  autoGenerate: false

# Optional: source the Postgres password from an external Secret.
# database:
#   postgres:
#     password:
#       existingSecret: mautrix-googlechat-postgres-password
#       existingSecretKey: password
